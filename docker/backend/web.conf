<VirtualHost *:80>
	ServerName ticketing.test
	DocumentRoot "/var/www/html/frontend/web"

	<Directory "/var/www/html/frontend/web/">
		RewriteEngine on
		AllowOverride All

		RewriteCond %{REQUEST_FILENAME} !-f
		RewriteCond %{REQUEST_FILENAME} !-d
		RewriteRule . index.php

		DirectoryIndex index.php
		Require all granted
	</Directory>
</VirtualHost>

<VirtualHost *:80>
	ServerName admin.ticketing.test
	DocumentRoot "/var/www/html/backend/web"

	<Directory "/var/www/html/backend/web/">
		RewriteEngine on
		AllowOverride All

		RewriteCond %{REQUEST_FILENAME} !-f
		RewriteCond %{REQUEST_FILENAME} !-d
		RewriteRule . index.php

		DirectoryIndex index.php
		Require all granted
	</Directory>
</VirtualHost>

<VirtualHost *:80>
	ServerName api.ticketing.test
	DocumentRoot "/var/www/html/api/web"

	<Directory "/var/www/html/api/web/">
		RewriteEngine on
		AllowOverride All

		RewriteCond %{REQUEST_FILENAME} !-f
		RewriteCond %{REQUEST_FILENAME} !-d
		RewriteRule . index.php

		DirectoryIndex index.php
		Require all granted
	</Directory>
</VirtualHost>